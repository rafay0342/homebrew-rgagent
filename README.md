# homebrew-rgagent

Homebrew tap for the RafayGen agentic CLI.

```bash
brew tap rafay0342/rgagent
brew install rgagent
```

Linux x86_64 for now (macOS/arm64 builds coming). Config lives in `~/.rgagent`.
